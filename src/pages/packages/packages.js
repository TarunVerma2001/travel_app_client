import PackagesInfo from '../../components/packagesIntro/packagesIntro';
import TourCards from '../../components/tourCards/tourCards';
import styles from './packages.module.css';

const Packages = () => {
    return ( <div>
        <PackagesInfo />
        <TourCards />
    </div> );
}
 
export default Packages;